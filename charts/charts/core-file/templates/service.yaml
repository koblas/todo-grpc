apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-file.fullname" . }}
  labels:
    {{- include "core-file.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "core-file.selectorLabels" . | nindent 4 }}
