apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-todo.fullname" . }}
  labels:
    {{- include "core-todo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "core-todo.selectorLabels" . | nindent 4 }}
