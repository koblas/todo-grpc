apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "publicapi-fileput.fullname" . }}-test-connection"
  labels:
    {{- include "publicapi-fileput.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "publicapi-fileput.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
